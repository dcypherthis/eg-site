<grid>
  <div class="free-lessons">
    <h2>Free Lessons</h2>
    <ul>
      {{#each model.lessons as |lesson|}}
        <li class="free-lesson">
          {{link-to lesson.title 'free-lessons' lesson}}
        </li>
      {{/each}}
    </ul>
  </div>

  <div class="free-lessons">
    <h2>Available Courses</h2>
    <ul>
      {{#each model.courses as |course|}}
        <li class="free-lesson">
          {{link-to course.name 'course-previews.detail' course}}
        </li>
      {{/each}}
    </ul>
  </div>
</grid>
