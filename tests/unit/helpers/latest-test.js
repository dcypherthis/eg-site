import { latest } from 'embergrep-site/helpers/latest';
import { module, test } from 'qunit';

module('Unit | Helper | latest');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = latest([42]);
  assert.ok(result);
});
