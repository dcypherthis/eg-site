import { secToHuman } from 'embergrep-site/helpers/sec-to-human';
import { module, test } from 'qunit';

module('Unit | Helper | sec to human');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = secToHuman([42]);
  assert.ok(result);
});
